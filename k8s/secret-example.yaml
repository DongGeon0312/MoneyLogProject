# 도전(F-16) 예시 Secret 템플릿. 실제 값으로 바꾼 뒤 secret.yaml(gitignore 대상)로 복사해서 적용하세요.
# kubectl apply -f secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: moneylog-secret
type: Opaque
stringData:
  DB_PASSWORD: "changeme"
  JWT_SECRET: "changeme-please-use-a-long-random-string-at-least-32-chars"
